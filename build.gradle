apply plugin: 'idea'
apply plugin: 'java'

buildscript {
    repositories {
        google()
        jcenter()
    }
}


dependencies {
    implementation group: 'org.json', name: 'json', version: '20210307'
}